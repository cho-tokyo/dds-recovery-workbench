# CLAUDE.md - DDS Recovery Workbench

このファイルはClaude Codeに対するプロジェクト指示書です。**作業開始前に必ず全文を読んでください。**

---

## プロジェクト概要

**製品名**: DDS Recovery Workbench  
**目的**: DDSのデータ復旧業務（特に削除・初期化案件）を効率化する内製ソフトウェア  
**主要価値**: 「お客様希望リスト駆動型」の目標達成型復旧ワークベンチ

詳細は `docs/PRD.md` を参照。

## 重要文書（必読）

| 文書 | 内容 |
|---|---|
| `docs/PRD.md` | プロダクト要求仕様書（Phase 1スコープの全機能要件） |
| `docs/architecture.md` | アーキテクチャ概要、モジュール責務 |
| `docs/progress.md` | 現在の進捗、完了チャンクの記録 |
| `docs/first_chunk.md` | **最初に着手するチャンクの具体指示** |
| `docs/specs/README.md` | FS仕様書の置き場所と参照ルール |

---

## 設計哲学（厳守）

1. **読み込み専用**: ソースデバイス（顧客HDD/SSD）への書き込みAPIは**実装しない**。仮想修復のみ
2. **目標駆動**: お客様希望リストを中心オブジェクトとして全モジュールが動く
3. **段階的フォールバック**: ライブモード（軽量） → スキャンモード（重量）の順
4. **定量レポート**: 全結果は希望リストとの達成度マトリクスで出力
5. **業務統合**: 単体ツールではなく、案件管理から納品レポートまでの業務基盤

---

## エージェント運用ルール

`.claude/agents/` に3つの専門エージェントを定義しています。

| エージェント | 担当 | 起動タイミング |
|---|---|---|
| `builder` | Rustコード実装、チャンク単位（最大200行） | 新機能・新モジュール実装時 |
| `tester` | テスト実行、結果分析 | チャンク完了直後・モジュール完成時 |
| `progress-tracker` | 進捗記録、PRD達成度更新 | テスト合格報告後 |

### 実装ワークフロー（必須順序）

```
[新機能依頼]
  ↓
[1] builder → チャンク実装（≤200行、単体テスト3件以上）
  ↓
[2] tester → cargo test --lib -p <crate>
  ↓ (失敗)
  └ builder へ修正依頼（[1] へ戻る）
  ↓ (成功)
[3] progress-tracker → docs/progress.md 更新
  ↓
[4] 3〜5チャンク蓄積 → tester に結合テスト実行依頼
  ↓ (失敗)
  └ 該当チャンク特定 → builder へ修正依頼
  ↓ (成功)
[5] モジュール完成 → tester に E2E + カバレッジ計測依頼
  ↓
[6] progress-tracker → PRDの FR-XXX 完了マーク
```

### 並列実行可能なケース

以下は独立性があるので並列化を検討:
- 異なるFS（fs-ntfs / fs-exfat / fs-fat32）の同時実装
- 異なるファイル形式バリデータの同時実装
- builder（実装）と progress-tracker（ドキュメント更新）の同時実行

### 禁止事項

- ❌ 単体テストなしの実装コミット
- ❌ 1チャンクで200行超え（実装+テスト合計）
- ❌ 「テストは後で書きます」
- ❌ 結合テスト未確認のままモジュール完成宣言
- ❌ ソースデバイスへの書き込み機能の実装
- ❌ GPL OSS（ntfs-3g, TestDisk等）のコード流用（参考のみ可）

---

## チャンク実装ルール

### 1チャンクの定義

実装 + 単体テストの合計が**最大200行**。これを超える場合は分割。

### 良い分割の例

- 1つのパーサ関数 + 関連構造体
- 1つの属性タイプ（NTFS の `$FILE_NAME` 等）
- 1つの戦略パターン実装

### 悪い分割の例

- 「NTFSパーサ全部」
- 「複数の属性を一気に」
- 「テストは別チャンクで」

### チャンク完了条件（全て満たすこと）

1. ✓ 実装が `cargo check -p <crate>` でエラーなし
2. ✓ 同ファイル内に `#[cfg(test)] mod tests` を配置
3. ✓ 単体テスト最低3件、`cargo test --lib -p <crate>` 合格
4. ✓ 全公開関数に rustdoc コメント
5. ✓ 関連する FR-XXX 要件をコメントに記載

---

## テスト戦略

| レベル | 目的 | 速度 | 配置 |
|---|---|---|---|
| 単体 | 関数・構造体の正しさ | ms単位 | `#[cfg(test)] mod tests` インライン |
| 結合 | モジュール組合せ、I/O含む | 秒単位 | `crates/*/tests/` |
| E2E | パイプライン全体 | 分単位 | `tests/` (workspace直下) |

### 結合テスト実行タイミング

3〜5チャンク完了するごとに `cargo test -p <crate>` で結合テストを実行。

### フィクスチャ使用

`fixtures/images/` に検証用ディスクイメージを配置。各イメージには対応する `.json`（ground truth）が必須。

`fixtures/scripts/` のPythonスクリプトで生成します（Linux環境必要）。

---

## 技術スタック

| レイヤ | 技術 |
|---|---|
| 言語（コア） | Rust 1.75+ |
| 言語（UI） | TypeScript 5.x + React 18 |
| UIフレームワーク | Tauri 2.x |
| DB | SQLite 3.40+ (WALモード) |
| バイナリパース | binrw |
| 非同期ランタイム | tokio |
| エラー型 | thiserror, anyhow |
| ロギング | tracing |
| テスト | cargo test, proptest, insta |

---

## クレート構成

```
crates/
├── core/           ← 共通型、エラー定義
├── disk-io/        ← Raw disk access抽象化
├── fs-common/      ← FS共通インタフェース（trait定義）
├── fs-ntfs/        ← NTFS reader
├── fs-exfat/       ← exFAT reader
├── fs-fat32/       ← FAT32 reader
├── diagnostic/     ← 診断エンジン
├── wish-match/     ← 希望リスト突合
├── recovery/       ← 復旧パイプライン
├── quality/        ← 品質判定エンジン
├── validators/     ← ファイル形式別バリデータ
├── report/         ← レポート生成
├── db/             ← SQLite操作
└── case-manager/   ← 案件管理
```

各クレートは独立してビルド・テスト可能であること。

---

## 安全要件（critical）

このソフトは顧客データを扱うため、以下を絶対遵守:

1. **書き込み禁止**: ソースデバイスを開く時は型レベルで `ReadOnly` 制約
2. **出力先強制分離**: 復旧データの出力先がソースと同一の場合はエラー
3. **失敗時の保護**: I/Oエラー時もソースデバイスに影響を与えない
4. **監査ログ**: 全操作を `tracing` で構造化ログに記録

これらに違反するコードを書こうとした場合、即停止して人間に確認すること。

---

## 開始指示

**最初に着手すべき作業は `docs/first_chunk.md` に記載されています。**

`docs/first_chunk.md` を読んで、Chunk 1 の実装から開始してください。

---

## 質問が必要な場合

以下のケースでは推測せず必ず確認:

- 仕様書に明記されていないFS挙動
- お客様データを扱う処理の安全性に疑問がある場合
- 複数の実装方針が考えられる場合
- 200行を超える可能性のあるチャンク分割の判断

## Git運用ルール

### ブランチ戦略
- `main`: テスト全合格状態のみ
- `chunk/NN-<short-name>`: 各チャンクの作業ブランチ
- builderエージェントは worktree 経由でブランチを切る

### コミットメッセージ
- 形式: `<type>: <description>`
- type: `feat` / `fix` / `test` / `docs` / `refactor` / `chore`
- 例: `feat(fs-ntfs): add boot sector parser (Chunk 4)`

### チャンク完了時のフロー
1. builder: feature ブランチでチャンク実装
2. tester: テスト実行（合格時のみ次へ）
3. progress-tracker: progress.md 更新
4. main へマージ（自動 or 手動レビュー後）

### コミット禁止物
- 顧客データ（PII含む）
- 著作権物（書籍PDF、ライセンス付きOSS）
- 大容量バイナリ（実HDDイメージ等）
- 認証情報（APIキー、パスワード、証明書）

.gitignoreで一次的に防御済み。git status で確認してからコミット。
